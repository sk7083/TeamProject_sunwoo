<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.kh.springTest.dao.MemberDAO">
	
	<!-- 회원 전체 정보 불러오는 쿼리문 -->
  <select id="selectList" resultType="kr.kh.springTest.vo.MemberVO">
    select me_id, me_pw, me_email, me_birth, me_auth, me_name, me_phone, me_address from member
  </select>
  	<!-- 회원가입 쿼리문 -->
  <insert id="insertMember">
  	insert into member(me_id, me_pw, me_email, me_birth, me_name, me_phone, me_address)
  	 values(#{me_id}, #{me_pw}, #{me_email}, #{me_birth}, #{me_name}, #{me_phone}, #{me_address}) 
  </insert>
  	<!-- 로그인 쿼리문 -->
  <select id="login" resultType="kr.kh.springTest.vo.MemberVO">
  	select me_id, me_pw from member
  	 where me_id = #{me_id} and me_pw = #{me_pw}
  </select>
  
  <!-- 로그인 세션 -->
  <select id="selectMemberById" resultType="kr.kh.springTest.vo.MemberVO">
  	select * from member where me_id = #{me_id}
  </select>
  
  <!-- 회원정보 수정 -->
  <update id="MemberUpdate">
  		update member
  				set
  					me_pw = #{me_pw},
  					me_email = #{me_email},
  					me_birth = #{me_birth},
  					me_auth = #{me_auth},
  					me_name = #{me_name},
  					me_phone = #{me_phone},
  					me_address = #{me_address}
  				where
  					me_id = #{me_id}
  </update>
  
  	<!-- 회원 상세 페이지 -->
	<select id="MemberSelect" resultType="kr.kh.springTest.vo.MemberVO">
		select me_id, me_pw, me_email, me_birth, me_auth, me_name, me_phone, me_address from member
			where me_id = #{me_id}
	</select>
	
	<!-- 회원 삭제 -->
  	<delete id="MemberDelete">
  		delete from member
  			where me_id = #{me_id}
  	</delete>

</mapper>
